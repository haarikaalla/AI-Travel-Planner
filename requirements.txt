# ─────────────────────────────────────────────────────────────
#  AI Travel Planner — runtime dependencies
#  Install:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# ── Agent orchestration ──────────────────────────────────────
langgraph>=0.2.60
langgraph-checkpoint-sqlite>=2.0.1
langchain>=0.3.13
langchain-core>=0.3.28
langchain-community>=0.3.13

# ── LLM providers (all optional at runtime — import-guarded) ──
langchain-ollama>=0.2.2            # local, free, default
langchain-groq>=0.2.3              # fastest cloud inference
langchain-google-genai>=2.0.7      # Gemini
langchain-openai>=0.2.14           # GPT / OpenRouter / any OpenAI-compatible
langchain-anthropic>=0.3.1         # Claude

# ── Validation & config ──────────────────────────────────────
pydantic>=2.10.4
pydantic-settings>=2.7.0
python-dotenv>=1.0.1

# ── Resilience & real-world data ─────────────────────────────
tenacity>=9.0.0
httpx>=0.28.1

# ── UI ───────────────────────────────────────────────────────
streamlit>=1.41.1
pandas>=2.2.3

# ── Export ───────────────────────────────────────────────────
reportlab>=4.2.5
